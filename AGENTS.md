# 项目开发规范

## 项目与工作方式

- 项目为原生微信小程序 TypeScript + NestJS 11 + MySQL，前后端同仓库。
- `miniprogram/` 为前端，`server/` 为后端；使用 npm workspaces 和现有 package-lock.json。
- 开始工作先阅读本文件、README、相关模块与脚本，沿用项目风格，保持修改范围小，不覆盖用户改动。
- 不自行引入新的框架、UI 库、状态库、ORM 或测试库。数据库访问沿用 mysql2 参数化查询。
- 不自动 commit，不使用危险 git 命令，不将真实凭据、生产地址或个人绝对路径写入业务代码。

## 数据库公共字段

所有表统一包含以下字段（包括会话表和进度表）：

- `id BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY`：数字自增主键。
- `created_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3)`：创建时间。
- `updated_at DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3)`：修改时间。
- `is_del TINYINT UNSIGNED NOT NULL DEFAULT 0`：0 未删除，1 已删除。禁止使用 deleted_at 替代。
- 外键统一使用 BIGINT UNSIGNED，与主表类型一致。保留外键约束，不配置级联物理删除。
- 字符集 utf8mb4；稳定导入标识及微信身份等大小写敏感字段使用明确的二进制比较规则。
- 应用和管理脚本的数据库连接会话使用北京时间（UTC+08:00）；时间字段区分创建/修改时间与真实业务活动时间。

## ID 与内容导入

- 数据库 `id`、内容清单 `sourceKey`（对应数据库 `source_key`）、排序字段分别承担身份、幂等导入和显示顺序，不得混用。
- 数据库 BIGINT 经 mysql2 配置为字符串读取；接口使用正整数字符串，不得经 Number/parseInt 转换 ID，也不允许前端通过 sourceKey 访问 ID 接口。
- 原始稳定字符串标识保存在 source_key：合集全局唯一；单集在合集内唯一；条目在单集内唯一。
- 唯一索引不加入 is_del。重复导入需匹配已有记录并保留数字主键，不能删除再插入、使用 REPLACE INTO 或自动恢复软删除内容。
- sourceKey 一经分配不可随排序重算；跨集重复表达正常保留。
- 导入默认为 dry-run；新增、更新、发布状态变化、缺失和已删除冲突都应报告。已有数据变化须显式确认差异后应用。
- 数据库发布状态默认草稿；内容清单可显式声明发布状态，dry-run 必须展示发布范围，导入不能无条件发布全部记录。
- 导入先按 sourceKey 解析真实数字外键，不使用可能损失精度的 insertId。以单集为事务边界，失败回滚；并发导入同一合集应串行化。
- 历史字符串主键表不能直接运行新版初始化 SQL 覆盖；须另行审查迁移路径。

## 发布状态、软删除和查询

- 合集和单集 `status`：0 草稿、1 已发布。发布与软删除独立，禁止继续用 active 同时表达二者。
- 双语条目随单集发布，不单设发布状态。常规查询过滤 is_del=0。
- 公开内容、目录、统计、复习、学习位置与进度查询必须检查完整父子关联：句子未删除，单集和合集均未删除且已发布。
- 已软删除内容不展示、不纳入当前统计，但不物理清空历史学习记录。
- 用户被软删除后不得自动重新注册或恢复，登录及现有会话均拒绝访问。
- 保存进度、修改资料和导入时同样检查软删除；遇到已删除进度返回冲突，不能通过 upsert 隐式恢复。
- 软删除不替代会话 expires_at（过期）与 revoked_at（撤销）。

## 学习业务字段

- 单集进度 `(user_id, episode_id)` 唯一；last_sentence_id 必须属于对应单集且可见。
- completed_at 为空表示未完成；不另存重复的完成状态。
- last_studied_at 表示真实学习活动，最近学习列表不使用 updated_at，避免维护操作扰乱排序。
- 条目进度 `(user_id, sentence_id)` 唯一；数据库 status 为 0 未练习、1 还不熟、2 已掌握。
- API 保留 unseen/learning/mastered 语义枚举，后端集中映射到数据库数值，前后端一致。
- last_reviewed_at 只在用户自评时更新；展开参考英文不改变掌握状态。
- 未掌握筛选包括未练习与还不熟；个人待复习仅包括还不熟。

## 接口与前端

- 外部输入必须校验，包括 ID、分页上限、字段长度、状态枚举、文件类型和资源归属。
- 用户身份只能从会话获得，不接受客户端指定其他用户 ID；令牌只保存摘要。
- 改字段时同步检查 SQL、导入数据与脚本、后端查询、前端类型和调用、测试、文档，禁止只改一端。
- 原生微信请求使用支持的方法，资料更新为 PUT，不使用 PATCH。
- 保留 loading、empty、error、disabled、success 等状态；失败保存必须显示未同步并可重试。
- 登录与头像昵称填写分开；头像持久化，不能保存手机临时文件路径。
- 小程序默认中文可见、参考英文隐藏；第一版不展示学习图片，不将参考英文当唯一答案。

## 安全与执行边界

- `.env`、数据库密码、AppSecret 等仅保存在后端本地或部署环境；不输出到日志或前端，不提交 Git。
- 用户当前只授权修改文件，未授权执行建表、导入或数据库迁移。后续写数据库前须取得明确授权，不能由“改代码”推定。
- 提供可审阅 SQL 和 dry-run；授权后才执行对应写操作。生产迁移、部署、权限调整另行确认。
- 不修改既有数据库账号权限，不自动清理共享数据库中的数据。

## 测试与交付

- 开发运行 `npm run dev`；相关校验使用 `npm run typecheck`、`npm test`、`npm run build`。
- 内容本地校验：`npm run content:validate --workspace server`，不连接数据库。
- 按变化增加有意义的测试：BIGINT 边界、软删除及父级可见性、导入幂等与拒绝恢复、状态映射和身份隔离。
- 默认测试使用隔离替身，不访问或写真实数据库。共享库上的写入测试需要授权。
- 如无法执行真实数据库/微信联调，明确说明限制，不把类型检查和替身测试说成真实集成通过。
- 最终简要说明修改内容、验证结果、未执行事项和关键文件路径。

## 对话补全

- 原文空的 AI 标记是待补写的中英文台词，不得过滤或用泛化套话批量填充。按整集上下文承接，保留 AI 开场及没有 AI 标记的连续用户台词。
- el_sentences.speaker：0 你，1 AI，默认 0。角色不能通过文本前缀临时猜测。
- 补写新增 source_key，已有句子标识、文本和进度保留，仅显示序号可调整；不删表、不重建已有句子。
- 生成内容必须有标记位置及来源记录，不宣称为原视频转录。未完成或未审核的草稿不得替换正式导入清单。

## 语音资源

- 线上语音通过 el_sentence_audio 关联句子数字 ID；sourceKey 只用于导入解析，文件名使用 SHA-256。
- 英文哈希不匹配时不提供音频；同样检查句子、单集、合集的发布与软删除状态。
- 宿主机音频目录只读挂载到 API 容器，音频文件不打包进镜像，不全量常驻内存。
- 更新默认预览，显式 apply 后先校验并安装文件，再事务更新映射；不能覆盖旧文件或自动恢复软删除。
- 音频映射与镜像独立；回退镜像不等于回退映射。生成与文件校验不能被当作人工试听或真机验证。
