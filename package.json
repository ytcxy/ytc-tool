{
  "name": "my-project",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "miniprogram",
    "server"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "npm run start:dev --workspace server",
    "build": "npm run build --workspace server",
    "typecheck": "npm run typecheck --workspaces",
    "test": "npm run test --workspace server"
  },
  "overrides": {
    "multer": "2.3.0"
  }
}
